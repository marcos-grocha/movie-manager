<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Movie Manager" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="<%= asset_path 'favicon-32x32.png' %>" type="image/png">
    <link rel="icon" href="<%= asset_path 'Mred-icon.ico' %>" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <ul>
      <li>
        <%= link_to "Home", root_path %>
      </li>
      <li>
        <%= link_to "Cadastrar novo Filme", new_movie_path %>
      </li>
      <li>
        <%= link_to "Cadastrar novo Diretor", new_director_path %>
      </li>
      <li>
        <%= link_to "Cadastrar novo Genero", new_genre_path %>
      </li>
    </ul>

    <%= yield %>
  </body>
</html>
